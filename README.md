#install

{
  "mcpServers": {
    "mcp-server": {
      "command": "uvx",
      "args": [
        "--from",
        "git+https://github.com/drkhannah/mcp-deployment",
        "mcp-server"
      ]
    }
  }
}
